var input = require('fs').readFileSync('/dev/stdin', 'utf8');
var lines = input.split('\n');
const a = parseInt(lines.shift());
let f = 1;

for(let i = 1; i < a; i++ ){
  f *= (a + 1) - i;
}
console.log(f);
